import { LoginInputDirective } from './login-input.directive';

describe('LoginInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginInputDirective();
    expect(directive).toBeTruthy();
  });
});
